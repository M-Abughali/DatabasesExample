<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
          package="com.ghalym.databasesexample">

    <application
            android:allowBackup="true"
            android:icon="@mipmap/ic_launcher"
            android:label="@string/app_name"
            android:roundIcon="@mipmap/ic_launcher_round"
            android:supportsRtl="true"
            android:theme="@style/AppTheme">
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>

                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>



        <activity android:name=".realmExample.Note.addNote.AddNoteActivity"/>
        <activity android:name=".realmExample.Note.displayNote.DisplayNotesActivity"/>
        <activity android:name=".realmExample.Note.updateNote.UpdateNoteActivity"/>
        <activity android:name=".realmExample.Note.NoteActivity"/>


        <activity android:name=".sqliteExample.Note.addNote.AddNoteActivity"/>
        <activity android:name=".sqliteExample.Note.displayNote.DisplayNotesActivity"/>
        <activity android:name=".sqliteExample.Note.updateNote.UpdateNoteActivity"/>
        <activity android:name=".sqliteExample.Note.NoteActivity"/>
    </application>

</manifest>